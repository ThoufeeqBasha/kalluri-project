name: applicant_manager
description: A simple Flutter CRUD application

publish_to: 'none' # Remove this if publishing

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # 🔥 FOR API CALLS (VERY IMPORTANT)
  http: ^1.2.1

  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # 🔥 IF YOU ADD IMAGES LATER
  assets:
    - assets/images/logo.jpeg